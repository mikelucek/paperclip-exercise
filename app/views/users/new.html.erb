<h2>new.html.erb</h1>

<%= form_for @user, :url => users_path, :html => { :multipart => true } do |x| %>
<%= x.file_field :avatar %>
<%= x.submit :create, data: {disable_with: "Uploading..."} %>
<% end %> 